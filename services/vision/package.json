{
  "name": "@plate-clean/vision",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8001",
    "start": "gunicorn app.main:app -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8001",
    "lint": "ruff check app tests",
    "lint:copy": "echo 'no copy-lint for vision service'",
    "typecheck": "mypy app",
    "test": "pytest",
    "build": "echo 'python package — no build step'"
  }
}
